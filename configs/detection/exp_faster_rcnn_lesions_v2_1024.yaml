# ==========================================================
# Detection Experiment Configuration: Faster R-CNN (Round 2: 1024x1024 + CLAHE + Micro Anchors)
# ==========================================================

dataset:
  path: data/raw
  annotation_file: train.txt
  image_directory: train
  num_classes: 5

detection_dataset:
  xml_dir: data/raw/lesion_detection/train
  image_dir: data/raw/train
  num_classes: 5

detection_model:
  architecture: fasterrcnn_resnet50_fpn
  pretrained: true
  num_classes: 5
  score_thresh: 0.20
  nms_thresh: 0.35
  min_size: 1024
  max_size: 1024

preprocessing:
  pipeline: [resize, normalize, to_tensor]
  resize:
    height: 1024
    width: 1024
  normalize:
    mean: [0.485, 0.456, 0.406]
    std: [0.229, 0.224, 0.225]

model:
  architecture: fasterrcnn_resnet50_fpn
  pretrained: true
  num_classes: 5

loss:
  name: cross_entropy

optimizer:
  name: adamw
  learning_rate: 0.0001
  weight_decay: 0.0005

scheduler:
  name: cosine
  eta_min: 1.0e-6

training:
  epochs: 35
  device: cuda
  deterministic: true

mixed_precision:
  enabled: true
  dtype: float16

dataloader:
  batch_size: 2
  num_workers: 2
  shuffle: true
  pin_memory: true
  persistent_workers: false
  drop_last: false

checkpoint:
  directory: experiments/exp_faster_rcnn_lesions_v2_1024/checkpoints
  monitor: map_50

early_stopping:
  enabled: true
  patience: 10
  monitor: map_50

logging:
  log_every_n_steps: 10

metrics:
  primary: [map_50, map_75]
  secondary: [map]
  per_class: true

experiment:
  name: exp_faster_rcnn_lesions_v2_1024
  seed: 42
