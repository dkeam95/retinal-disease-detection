# ==========================================================
# Detection Experiment Configuration: Faster R-CNN (Round 3: Maximum 1536x1536 Resolution + CLAHE + Micro Anchors)
# ==========================================================

dataset:
  path: data/raw
  annotation_file: train.txt
  image_directory: train
  num_classes: 5

detection_dataset:
  xml_dir: data/raw/lesion_detection/train
  image_dir: data/raw/train
  num_classes: 5

detection_model:
  architecture: fasterrcnn_resnet50_fpn
  pretrained: true
  num_classes: 5
  score_thresh: 0.20
  nms_thresh: 0.35
  min_size: 1536
  max_size: 1536

preprocessing:
  pipeline: [resize, normalize, to_tensor]
  resize:
    height: 1536
    width: 1536
  normalize:
    mean: [0.485, 0.456, 0.406]
    std: [0.229, 0.224, 0.225]

model:
  architecture: fasterrcnn_resnet50_fpn
  pretrained: true
  num_classes: 5

loss:
  name: cross_entropy

optimizer:
  name: adamw
  learning_rate: 0.00008
  weight_decay: 0.0005

scheduler:
  name: cosine
  eta_min: 1.0e-6

training:
  epochs: 40
  device: cuda
  deterministic: true

mixed_precision:
  enabled: true
  dtype: float16

dataloader:
  batch_size: 1
  num_workers: 2
  shuffle: true
  pin_memory: true
  persistent_workers: false
  drop_last: false

checkpoint:
  directory: experiments/exp_faster_rcnn_lesions_v3_1536/checkpoints
  monitor: map_50

early_stopping:
  enabled: true
  patience: 10
  monitor: map_50

logging:
  log_every_n_steps: 10

metrics:
  primary: [map_50, map_75]
  secondary: [map]
  per_class: true

experiment:
  name: exp_faster_rcnn_lesions_v3_1536
  seed: 42
